package usaco;

import java.io.BufferedReader;
import java.io.FileInputStream;
import java.io.FileOutputStream;
import java.io.IOException;
import java.io.InputStreamReader;
import java.io.OutputStreamWriter;
import java.io.PrintWriter;

public class div7 {

	public static void main(String[] args) throws IOException {
		BufferedReader br = new BufferedReader(new InputStreamReader(new FileInputStream("pails.in")));
		PrintWriter pw = new PrintWriter(new OutputStreamWriter(new FileOutputStream("pails.out")));
		//BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		//PrintWriter pw = new PrintWriter(new OutputStreamWriter(System.out));
		
		int x = Integer.valueOf(br.readLine());
		int[] arr = new int[x];
		
		//do a prefix sum that is mod 7 and just find the two farthest ones that are the same
		//0 just is the index
		
		
		//prefix of all the cows id not the number but how many times they appear
		//get the range
		
		
 		
	}

}
